# Exact versions this repository was built, trained, evaluated and tested against.
#
# Reproduced on: Python 3.11.9 (CPython), Windows 11 AMD64
# Verified under these versions:
#   - dataset regenerates to identical SHA-256 digests (data/synthetic/checksums.json)
#   - model retrains to identical metrics (reports/task11_model_results.json)
#   - reports/task11_policy_evaluation.json and reports/task12_robustness.json reproduce
#   - full test suite passes
#
# Install with:  pip install -r requirements-lock.txt
#
# Note: models/recovery_predictor.joblib is a pickled scikit-learn estimator and is
# tied to scikit-learn 1.9.0. Under a different version, retrain instead of
# force-loading:  python scripts/train_recovery_model.py

numpy==2.3.2
pandas==2.3.2
scikit-learn==1.9.0
scipy==1.17.1
PyYAML==6.0.3
matplotlib==3.11.1
joblib==1.5.3
fastapi==0.139.2
starlette==1.3.1
pydantic==2.13.4
uvicorn==0.51.0
httpx==0.28.1
pytest==9.1.1
